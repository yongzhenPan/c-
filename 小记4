c++11,对右值新增加的将亡值，适于将右值引用绑定到将亡值的场景，主要是移动构造函数（移动赋值运算符）中，假定要被移动的对象（A）将亡，使用move(A)返回右值，作为参数传递到移动构造函数中，我们在移动构造函数中让我们的新对象接管右值引用指向的内存，然后通过改变右值引用的某些数据成员等等来使它处于可析构状态（因为我们是假定它将亡，实际上它并不是一个真正的将亡对象，所以我们要把戏做到底，就要手动让它将亡）。
