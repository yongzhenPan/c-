在VC中如果你没有把指针初始化的话，那编译器会把指针指向地址变为0xCCCCCCCC，一个不可访问的地址

在继承中派生类（A）可以通过作用域运算符使用基类(B)成员函数（fun()）: B::func()
在普通类与类的关系里面是不存在这样的调用的，因为成员函数的调用是需要对象来调用它的，在继承关系中派生类对象中是包含了基类的对象，可以使用A中的B来调用fun(),（A其实也是在B的作用域中的）而普通类中就不要出现这种用其他类的作用域来调用成员函数写法。
就像string源码中可以直接 allocator<char>::allocate()，string和allocator<T>的关系？
